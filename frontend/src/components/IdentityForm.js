import React from 'react';

const IdentityForm = () => {
	return (
		<form>
			<input type="text" placeholder="Enter identity" />
			<button type="submit">Create Identity</button>
		</form>
	);
};

export default IdentityForm;